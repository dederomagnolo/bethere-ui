import {
  FaLeaf as Leaf,
} from 'react-icons/fa'

import {
  BiStation as SensorIcon
} from 'react-icons/bi'

export const cards: any = {
  sensor: {
    label: 'Sensor',
    icon: SensorIcon
  },
  watering: {
    label: 'Irrigação',
    icon: Leaf,
  }
}
